#include "MBotController.h"

#include UE_INLINE_GENERATED_CPP_BY_NAME(MBotController)

AMBotController::AMBotController(const FObjectInitializer& ObjectInitializer)
: Super(ObjectInitializer)
{
}

// Called when the game starts or when spawned
void AMBotController::BeginPlay()
{
	Super::BeginPlay();
	
}

// Called every frame
void AMBotController::Tick(float DeltaTime)
{
	Super::Tick(DeltaTime);
}

